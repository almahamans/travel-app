import { handleSubmit } from '../js/formHandler'

describe('test jandle sumbit function', () => {
    test('Check handleSubmit function is not undefined', () => {
        expect(handleSubmit).toBeDefined()
    })
})